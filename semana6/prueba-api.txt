Cuando se envía una petición POST a la ruta /registro con un JSON que contiene un nombre y un mensaje, el servidor responde con un objeto JSON donde confirma que los datos fueron recibidos.

Además, devuelve los mismos valores enviados (nombre y mensaje), lo que permite verificar que la información llegó correctamente al backend y fue procesada por Express.